import { Text } from 'react-native'; import { router } from 'expo-router'; import { useAuthStore } from '@/stores/auth'; import { Button, Card, Screen, s } from '@/components/ui'; import { signOut } from '@/services/auth';
export default function Profile(){const user=useAuthStore(s=>s.user); return <Screen><Card><Text style={s.title}>Perfil público</Text><Text>{user?.email}</Text><Button title="Editar perfil" onPress={()=>router.push('/settings/profile')}/><Button title="Planos" variant="secondary" onPress={()=>router.push('/subscription/plans')}/><Button title="Sair" variant="danger" onPress={signOut}/></Card></Screen>}
