import { Text } from 'react-native'; import { router } from 'expo-router'; import { PLAN_FEATURES, PLAN_LIMITS } from '@/constants/plans'; import { Button, Card, Screen, s } from '@/components/ui';
export default function Plans(){return <Screen><Text style={s.title}>Planos Possível</Text>{(['free','pro'] as const).map(plan=><Card key={plan}><Text style={s.subtitle}>{plan==='pro'?'Possível Pro':'Gratuito'}</Text><Text>Anúncios: {PLAN_LIMITS[plan].activeListings} · Armazenamento: {PLAN_LIMITS[plan].storageGb} GB · Chamadas: {PLAN_LIMITS[plan].callParticipants} pessoas/{PLAN_LIMITS[plan].callDurationMinutes} min</Text>{PLAN_FEATURES[plan].map(f=><Text key={f}>• {f}</Text>)}{plan==='pro'?<Button title="Assinar Pro" onPress={()=>router.push('/subscription/checkout')}/>:null}</Card>)}</Screen>}
