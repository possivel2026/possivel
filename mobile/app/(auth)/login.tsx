import { useState } from 'react'; import { Text, TextInput } from 'react-native'; import { router } from 'expo-router'; import { Button, Card, Screen, s } from '@/components/ui'; import { signIn } from '@/services/auth';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''); async function submit(){try{await signIn({email,password}); router.replace('/(tabs)')}catch(e:any){setError(e.message)}} return <Screen><Card><Text style={s.title}>Entrar</Text><TextInput accessibilityLabel="E-mail" style={s.input} autoCapitalize="none" value={email} onChangeText={setEmail} placeholder="voce@email.com"/><TextInput accessibilityLabel="Senha" style={s.input} secureTextEntry value={password} onChangeText={setPassword} placeholder="Senha"/><Button title="Entrar" onPress={submit}/><Button title="Esqueci minha senha" variant="secondary" onPress={()=>router.push('/(auth)/forgot-password')}/>{error?<Text>{error}</Text>:null}</Card></Screen>}
