import { useState } from 'react'; import { Text, TextInput } from 'react-native'; import { router } from 'expo-router'; import { Button, Card, Screen, s } from '@/components/ui'; import { signUp } from '@/services/auth';
export default function Signup(){const [form,set]=useState({name:'',handle:'',email:'',password:''}),[error,setError]=useState(''); async function submit(){try{await signUp(form); router.replace('/(auth)/login')}catch(e:any){setError(e.message)}} return <Screen><Card><Text style={s.title}>Criar conta</Text>{(['name','handle','email','password'] as const).map(k=><TextInput key={k} accessibilityLabel={k} style={s.input} secureTextEntry={k==='password'} autoCapitalize="none" value={form[k]} onChangeText={v=>set({...form,[k]:v})} placeholder={k}/>) }<Button title="Criar minha conta" onPress={submit}/>{error?<Text>{error}</Text>:null}</Card></Screen>}
